<h1>Debate#show</h1>
<p>Find me in app/views/debate/show.html.erb</p>
<p>Create a new comment</p>

<%= form_for([@debate, @debate.points.new]) do |f| %>
     
      <%= f.hidden_field(:parent_id, :value => nil) %>
      <%= f.label :body %>
      <%= f.text_field :body %>

      <%= f.submit "Make a Point" %>
<% end %>
    
    
<%= @debate.summary %>

<% puts "anytest#{Debate.find(@debate.id).points.any?}" %>
<% if Debate.find(@debate.id).points.any? %>


<%= render partial: 'points/point', collection: @debate.points %>

<% end %>